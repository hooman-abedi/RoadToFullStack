const firstName = "Hooman";
const startingDate = "30-04-2026"
const nodeVersion = process.version;
const processId = process.pid;
console.log(`${firstName} started Month 1 on ${startingDate}.`);
console.log(`the node version is: ${nodeVersion}`);
console.log(`the process id is: ${processId}`);